Contracts to be defined when this service's domain logic is scoped.

The Task Router's real contract will be derived from
`TASK_ROUTER_SPECIFICATION.md` at the repo root in a later milestone — it is
intentionally not invented here.
