// Package internal holds domain logic for agent-presence: the live
// WebSocket connection registry (internal/registry), the WebSocket
// transport (internal/wsserver), and the Task Router event relay
// (internal/relay). See the service README for the full design.
package internal
